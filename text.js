/* var container = document.getElementById('tabla');

// Generar los divs con los números del 00 al 99
for (var i = 0; i < 100; i++) {
	var div = document.createElement('div');
	div.className = 'numero';
	div.textContent = ('0' + i).slice(-2); // Asegura que siempre tenga 2 dígitos
	container.appendChild(div);
}

// Obtener todos los elementos div con la clase "numero"
var elementos = document.querySelectorAll('.numero');

// Recorrer los elementos obtenidos
for (var i = 0; i < elementos.length; i++) {
	var estructuraHTML = elementos[i].outerHTML;
	console.log(estructuraHTML);
}

//despues lo copie de la consolta, lo puse en excel y filtre el text.17blabla de la columna
 */
